# Entrainement Angular

## Récuper le projet
- `git clone` permet de recupérer le projet

## Comment récuper le projet et lancer le projet
- `npm install` pour installer les modules installé.
- `npm start` pour lancer le serveur.

